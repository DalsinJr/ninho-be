<?xml version="1.0" encoding="UTF-8"?>
<!--
    Layout padrão do Spring Boot + requestId e usuário (id) via MDC, com a mensagem passando pelo
    MascaraPiiConverter: nenhum e-mail ou telefone em log (SPEC §7.3, §11.3).
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <conversionRule conversionWord="msgSemPii" converterClass="com.escolaamerica.ninho.shared.api.MascaraPiiConverter"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} [%X{requestId:-}] [%X{usuario:-}] %msgSemPii%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
